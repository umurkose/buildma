// All message templates, one roof. Routes import from '@/notify/templates'.
// Add a file per message under the email/ channel and re-export it here
// (e.g. email_welcome).
export { email_verification } from './email/verification'
